{
  "name": "videonotes",
  "version": "2.0.0",
  "description": "YouTube Video Notes with exact frame capture using Puppeteer",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "puppeteer": "^21.0.0"
  }
}
